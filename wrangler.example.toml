name = "nowplaying-hono"
main = "src/index.ts"
compatibility_date = "2023-03-21"

kv_namespaces = [
{ binding = "CACHE_TOKEN", id = "" },
{ binding = "TRACK_CACHE", id = "" }
]

# The necessary secrets are:
# - SPOTIFY_CLIENT_ID
# - SPOTIFY_CLIENT_SECRET
# Run `echo <VALUE> | wrangler secret put <NAME>` for each of these
